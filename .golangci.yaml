run:
  timeout: 30s  

linters:
  enable:
    - errcheck
    - gocognit
    - gofmt
    - gosec
    - gosimple
    - govet
    - staticcheck
    - unused

linters-settings:
  gocognit:
    min-complexity: 5
